Removing Elements from DirectScrolledList won't remove it from the list itself, only from the scenegraph, which can crash the app on further editing of the list.
-> Remove elements from DirectScrolledList as soon as they are removed from the scenegraph

Render problems in Scrolled frames

Drag and Drop doesn't respect parenting. Currently mouse Drag/Drop is limited to the canvas frame size and some elements extend those limits.
-> Make drag/drop check parent instead of canvas size

Resize Window could make visual Editor frame go out of view a bit
-> How to catch window events without breaking aspect2D?